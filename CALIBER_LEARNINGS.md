# Caliber Learnings

Accumulated patterns and anti-patterns from development sessions.
Auto-managed by [caliber](https://github.com/caliber-ai-org/ai-setup) — do not edit manually.

- **[gotcha]** When editing `packages/KNIRVBASE/go/internal/query/knirvql.go`, only import `encoding/binary` and `math` if functions that use them are actually present and called in the final file. Go rejects unused imports — missing this causes build failures that look unrelated to the query logic being added.
- **[gotcha]** `go test` in `packages/KNIRVBASE/go/` must be run from within that directory with relative package paths (e.g. `./pkg/nrv/...`). Running from the repo root gives "cannot find main module" because each KNIRV package has its own `go.mod`.
- **[correction]** `LSHSalt` does not exist as a separate field in the NRV wire spec — it IS the `GoldenSeed`. Never create or reference a separate `LSHSalt` field in `Bracket`; remove any test assertions on `decoded.LSHSalt` and rename any document index keys from `lsh_salt` to `golden_seed`.
- **[correction]** `GoldenSeed` encodes to exactly ONE wire location: bytes 61–64 (slot 11, offset 0x3D–0x40). Previous code incorrectly wrote it to both 0x2B–0x2E and 0x3D–0x40. The correct 80-byte wire map is: projections (0–31), SubSecondUS (32–35), syntactic packed byte (36), CPU reserved (37–38), DepHead (39), IntentFlags (40), DomainSig (41–42), Memory/context bridge (43–60), GoldenSeed (61–64), Z3/reserved (65–79).
- **[correction]** `Bracket.Memory` is 18 bytes (wire bytes 43–60, slots 6–8), not 14. Using `[14]byte` for Memory misaligns GoldenSeed out of its canonical slot at 61–64 and breaks the wire layout.
- **[correction]** Wire byte 36 (slot 4) is a single packed byte encoding POS (low nibble), tense, and plurality via `PackSyntacticByte`. Earlier incorrect layouts split these across bytes 36/37/38 as three separate fields. Always encode slot 4 as one packed byte and decode with `UnpackSyntacticByte`.
- **[fix]** In `FrameTicker.AppendBracket`, the anchor bracket (`lastIBkt`) must be stored via `cloneBracketShallow(b)`, not as a raw pointer. Storing the raw pointer means subsequent P-frame XOR mutations overwrite the anchor's `Projections` field, silently corrupting drift detection for all future brackets in the same frame.
- **[gotcha]** `packages/KNIRVSERVER/pkg/knirvagent/` is the KNIRVSERVER-side process manager (its own `go.mod`, module `github.com/KNIRV/KNIRV_NETWORK/KNIRVAGENT`). The KNIRVAGENT *binary source* lives at `packages/KNIRVAGENT/` (module `github.com/knirvcorp/knirvagent`). These are two distinct modules — editing one has no effect on the other.
- **[correction]** New library code for the KNIRVAGENT binary must go in `packages/KNIRVAGENT/pkg/<subpackage>/` subdirectories — not at the `cmd/` level and not in `packages/KNIRVSERVER/pkg/knirvagent/`. After changes, rebuild and re-embed: run `go build -o ../KNIRVSERVER/pkg/knirvagent/bin/knirvagent ./cmd/knirvagent/` from inside `packages/KNIRVAGENT/`, then rebuild KNIRVSERVER (which uses `//go:embed bin/knirvagent`).
- **[correction]** All internal inter-service communication uses Unix sockets exclusively; KNIRVGATEWAY (`:8888`) is the sole TCP listener the browser reaches. New KNIRVSERVER `backend/internal/` REST endpoints must be registered under `/api/v1/` (proxied by KNIRVGATEWAY via `backend.sock`); new WebSocket endpoints are registered at the root router (not under `/api/v1/`) and require a separate WebSocket tunnel rule in KNIRVGATEWAY.
- **[gotcha]** `newSocketProxy()` in `packages/KNIRVGATEWAY/internal/server/server.go` uses `httputil.ReverseProxy` which strips `Upgrade` and `Connection` headers — it cannot tunnel WebSocket connections. The gateway currently has no `/ws/*` proxy rule, so all browser WebSocket connections silently fail at the gateway. Fix requires raw TCP hijacking via `http.Hijacker` (not `ReverseProxy`) for any `/ws/` path prefix.
- **[fix]** When `mcp__plugin_socraticode_socraticode__codebase_search` returns empty `{}` for DVE or KNIRVAGENT queries, the index is likely stale. Fall back immediately to `find <pkg_dir> -type f -name "*.go" | xargs grep -l <term>` to locate relevant files by content.
- **[convention]** KNIRVSERVER REST API routes are versioned under `/api/v1/`. WebSocket routes are registered at the root router outside `/api/v1/` and follow the pattern `/ws/dve/{nodeId}/...`. Do not register WS routes under the versioned prefix.
- **[gotcha]** `NewDVEProxyHandler` in `dve_proxy_handlers.go` loads templates via `template.ParseGlob("backend/templates/dve/*.gohtml")` — a relative path. If the backend_server process CWD is not the directory containing `backend/`, this glob fails and the WARN log is the only signal. Verify `os.Getwd()` on backend startup if DVE pages return 500 or 404.
- **[gotcha]** `dve-verification-iframe.tsx` port-scans `['8081', '8080', '8090']` to discover the gateway. In testnet/production the gateway runs on `:8888`. Set env var `NEXT_PUBLIC_GATEWAY_PORT=8888` (matching the configured gateway port) so the iframe finds the gateway without scanning the wrong ports.
- **[pattern]** Implementation spec HTML files at the repo root (`DVE_PAGES_FIX.html`, `agentic_hierarchy_implementation.html`) describe the intended architecture for DVE features but may lag or lead actual code. Always grep for handler registration and file existence before assuming a spec gap — phases described as "not yet implemented" in the spec are often already wired in `main.go`.
- **[gotcha]** KNIRVHASHER uses TWO distinct Unix sockets: `hasher.sock` (HTTP, headless pipeline run/stop/status) and `hasher-grpc.sock` (gRPC, for data-connector `ExportSecurityData`/`TriggerTraining` calls). Connecting a gRPC client to `hasher.sock` produces the misleading error `http2: failed reading server preface` — it looks like a server crash but is a protocol mismatch. The gRPC socket path is derived as `<hasher_socket_path_without_.sock>-grpc.sock` when `HASHER_GRPC_SOCKET_PATH` is unset.
- **[env]** KNIRVHASHER socket env vars: `HASHER_SOCKET_PATH` → HTTP headless socket; `HASHER_GRPC_SOCKET_PATH` → gRPC socket. The data-connector subprocess only receives `HASHER_GRPC_SOCKET_PATH` if `knirvhasher.ManagerConfig.GRPCSocketPath` is set — it is not passed automatically. Always wire `hasherGRPCSocketPath` into `ManagerConfig.GRPCSocketPath` when constructing the hasher manager in `main.go`. Also: `data-connector`'s `LoadConfig` reads `HASHER_GRPC_SOCKET_PATH` (not `HASHER_SOCKET_PATH`) to dial the server — using the HTTP socket env var here triggers a silent protocol failure.
- **[gotcha]** `HasherExporter` in `backend/internal/services/dve/hasher_export.go` defaults to `localhost:50051` when no address is passed — this TCP address is wrong; the backend's gRPC server is on a Unix socket. Any caller must pass `"unix:<hasher-grpc.sock>"` explicitly or the export stream will immediately fail with a connection error.
- **[gotcha]** KNIRVHASHER pipeline stages in `packages/KNIRVHASHER/pipeline/` each have their own `go.mod` with shorthand local module names (`data-connector`, `data-miner`, `data-encoder`, `data-trainer`). Each stage must be built and tested from within its own subdirectory — they cannot be imported from or built via the root `packages/KNIRVHASHER/` module.
- **[gotcha]** `packages/KNIRVHASHER/pipeline/2_DATA_ENCODER`'s `AutoDetectInputFile()` searches for its input in `$KNIRV_APP_DATA_DIR/json/` and `~/.local/share/hasher/`, in priority order: `ai_knowledge_base_goat_alpaca.json` → `ai_knowledge_base_alpaca.json` → `ai_knowledge_base.json` → `.arrow` variants. If the data-miner writes output to a different path than expected, the encoder silently falls back to a placeholder that doesn't exist and fails immediately with "input file does not exist".
- **[gotcha]** `packages/KNIRVHASHER/pipeline/0_DATA_CONNECTOR` connects to KNIRVSERVER via TCP gRPC (`grpc.NewClient(config.KNIRVSERVERAddr, grpc.WithTransportCredentials(insecure.NewCredentials()))`). To route through the Unix socket instead, the YAML config `knirvserver_addr` must use the `unix:` scheme (e.g. `unix:/path/to/hasher-grpc.sock`) — the address is passed verbatim to `grpc.NewClient`.
- **[gotcha]** Passing `--goat` to the knirvhasher CLI skips `0_DATA_CONNECTOR` entirely and fetches from HuggingFace (`stallone/goat` dataset) directly. Output filenames change from `ai_knowledge_base.json` to `ai_knowledge_base_goat_alpaca.json` — the encoder's `AutoDetectInputFile()` prefers the goat variant, so the pipeline still flows correctly. Remove `--goat` when you want the data-connector security export path instead.
- **[env]** `KNIRV_APP_DATA_DIR` must be set consistently for all KNIRVHASHER pipeline stages. `data-miner` writes to `$KNIRV_APP_DATA_DIR/hasher/data/json/`; `data-encoder` reads from `$KNIRV_APP_DATA_DIR/json/`. If the env var differs between stage invocations or is absent in any stage, each falls back to its own directory resolution logic and the stages write/read from different paths, silently breaking the pipeline.
- **[gotcha]** `data-trainer` (CGo binary) links against `libcuda_hash.so` at build time, but the `.so` is NOT automatically included in `internal/cli/embedded/bin/`. At runtime the binary fails immediately with `exit status 127` ("cannot open shared object file"). After `make build-data-trainer`, also ensure `libcuda_hash.so` is present in the embed dir. The updated Makefile `build-data-trainer` target copies it automatically — older installs or manual builds require: `cp pkg/hashing/methods/cuda/libcuda_hash.so internal/cli/embedded/bin/`.
- **[gotcha]** The `data-trainer` pipeline stage in `controller.go` (`PipelineStage.Args`) previously had `-hash-method cuda` hardcoded, causing `exit status 127` on any machine without a GPU even when `libcuda_hash.so` is present. Always use `-hash-method auto` — it falls back gracefully to the software implementation when CUDA is unavailable.
- **[gotcha]** When KNIRVSERVER runs as root (e.g., `sudo ./backend_server`), the `data-miner`'s embedded C++ Python bridge calls `site.getusersitepackages()` which resolves to `/root/.local/...`, not the invoking user's `~/.local/...`. spaCy models installed for the real user (like `en_core_web_sm`) become invisible and the pipeline fails with `OSError: Can't find model 'en_core_web_sm'`. Fix: `manager.go` now reads `SUDO_USER` and injects the correct `PYTHONPATH`; always start KNIRVSERVER via `sudo -E` or ensure `SUDO_USER` is preserved in the environment.
- **[gotcha]** `data-miner` has `RUNPATH` baked in at build time pointing to the absolute source-tree path (`packages/KNIRVHASHER/pipeline/1_DATA_MINER/spacy/lib`) for `libspacy_wrapper.so`. On deployed machines where the source tree doesn't exist (e.g., `/var/lib/knirvserver/bin/data-miner`), `ldd` will show `libspacy_wrapper.so => not found` and the NLP bridge silently fails to initialize. Either co-locate `libspacy_wrapper.so` with the binary or set `LD_LIBRARY_PATH` to include the source-tree lib path when running the deployed binary.
- **[gotcha]** KNIRVSERVER `packages/KNIRVSERVER/config/production.yaml` has eBPF monitoring, Cognitive Engine, and blockchain integration **disabled** via feature flags — these subsystems are fully implemented in code but not running in production by default. When investigating why these features aren't triggering in a production deployment, check feature flags in `production.yaml` before assuming the subsystem is broken or unimplemented.
- **[gotcha]** When creating or updating `.socraticodecontextartifacts.json`, verify each artifact path actually exists before adding it. `Write` succeeds even with non-existent paths, and SocratiCode silently skips artifacts that don't resolve — making omissions impossible to diagnose without re-running `codebase_status`. Use `find <path> -maxdepth 1` to confirm paths before inclusion.
- **[gotcha]** The `knirvarena.tar.gz` bundle (created via `tar -czf ... -C dist .`) includes a root `./` entry that resolves to `destDir` itself during extraction. The path-traversal guard in `packages/KNIRVSERVER/pkg/knirvarena/embedded.go` (`!strings.HasPrefix(cleanTarget, cleanDest+sep)`) always rejects this entry — returning "illegal tar path" — because `cleanTarget == cleanDest` without the separator. The fix is to add `if cleanTarget == cleanDest { continue }` before the HasPrefix check. Without this, KNIRVARENA fails to extract on first launch and the arena directory remains empty.
- **[gotcha]** KNIRVARENA is served at the `/arena/` sub-path, so hardcoded root-relative public asset paths (e.g. `/assets/avatar/Green_Bot_Explorer.glb`, `/sounds/background.mp3`) resolve to the server root rather than under `/arena/`. The browser receives an HTML 404 page in place of the binary file, which Three.js/GLTF reports as `"Unexpected token '<', '<!DOCTYPE'...is not valid JSON"` — with no obvious connection to a path prefix issue. Always use `` `${import.meta.env.BASE_URL}assets/...` `` for all public asset references in KNIRVARENA source (`AIAgent.tsx`, `AnchorStraighteningSequence.tsx`, `GameArena.tsx`, `AgentManager.tsx`, etc.).
- **[pattern]** KNIRVARENA rebuild-and-embed workflow after source changes: (1) `cd packages/KNIRVARENA && npm run build` (~90s), (2) `tar -czf /tmp/knirvarena_new.tar.gz -C dist .`, (3) `mv /tmp/knirvarena_new.tar.gz packages/KNIRVSERVER/pkg/knirvarena/bin/knirvarena.tar.gz`, (4) rebuild both KNIRVSERVER binaries: `cd packages/KNIRVSERVER/backend && go build -o ../bin/backend_server ./cmd/backend_server/` then `cd packages/KNIRVSERVER && go build -o dist/knirv-server .`. Use absolute paths in `go build` invocations — the shell CWD resets between Bash calls and a wrong CWD produces silent failure (empty output, no error) rather than a clear build error.
