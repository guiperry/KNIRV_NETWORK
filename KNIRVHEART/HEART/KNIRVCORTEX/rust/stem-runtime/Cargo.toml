[package]
name = "knirv-inner-runtime"
version = "0.1.0"
edition = "2021"
description = "Inner model runtime for KNIRV-CORTEX"

[lib]
name = "inner_runtime"
path = "src/lib.rs"
# cdylib for WASM builds (stem.wasm), staticlib for native linking
crate-type = ["staticlib", "cdylib"]

[dependencies]
shared-types = { path = "../shared-types", package = "knirv-cortex-shared-types" }
anyhow = "1.0"
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# WASM support for stem.wasm builds
wasm-bindgen = { version = "0.2", optional = true }

# For WASM compatibility
[target.'cfg(target_arch = "wasm32")'.dependencies]
wasm-bindgen = "0.2"
prost = "0.12"

# For now, use a minimal ML runtime
# In production, would use candle-core, candle-nn, candle-transformers
# candle-core = { version = "0.3", features = ["safetensors"] }
# candle-nn = "0.3"
# candle-transformers = "0.3"

[features]
default = []
wasm = []
