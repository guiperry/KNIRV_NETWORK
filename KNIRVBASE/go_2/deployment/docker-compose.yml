version: '3.8'

services:
  knirvchain-node:
    build: ..
    container_name: knirvchain-node
    ports:
      - "8080:8080"
      - "8081:8081"
      - "9090:9090"
    environment:
      - NODE_ENV=production
      - LOG_LEVEL=info
    volumes:
      - ./data:/var/lib/knirvchain
      - ./config.yaml:/root/config.yaml
      - ./wallet.key:/etc/knirvchain/wallet.key
    depends_on:
      - redis
      - postgres
    restart: unless-stopped
    networks:
      - knirvchain-network

  redis:
    image: redis:7-alpine
    container_name: knirvchain-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes
    restart: unless-stopped
    networks:
      - knirvchain-network

  postgres:
    image: postgres:15-alpine
    container_name: knirvchain-postgres
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=knirvchain
      - POSTGRES_USER=knirv
      - POSTGRES_PASSWORD=secure_password_here
    volumes:
      - postgres-data:/var/lib/postgresql/data
    restart: unless-stopped
    networks:
      - knirvchain-network

  prometheus:
    image: prom/prometheus:latest
    container_name: knirvchain-prometheus
    ports:
      - "9091:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus-data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
    restart: unless-stopped
    networks:
      - knirvchain-network

  grafana:
    image: grafana/grafana:latest
    container_name: knirvchain-grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - grafana-data:/var/lib/grafana
    restart: unless-stopped
    networks:
      - knirvchain-network

volumes:
  redis-data:
  postgres-data:
  prometheus-data:
  grafana-data:

networks:
  knirvchain-network:
    driver: bridge