hasher:
  addr: "/var/run/hasher.sock"  # Unix socket for container compatibility

source:
  org_id: "org_default"
  user_id: "all"

knirvbase:
  data_dir: "/var/knirvbase/hasher"      # shared database for all pipeline stages

processing:
  max_concurrent: 4
  pii_scrub: true
  deduplicate: true