# Caliber Learnings

Accumulated patterns and anti-patterns from development sessions.
Auto-managed by [caliber](https://github.com/caliber-ai-org/ai-setup) — do not edit manually.

- **[gotcha]** The `DVEURIRegistry` (`backend/internal/services/dve_uri_registry.go`) is an in-memory map that starts empty on every boot — DVEs created or tracked via `dveManager.NodeTracker` are NOT automatically registered in it. Any handler that exclusively calls `uriRegistry.GetByDVEID()` will always return 404 for live DVEs. Always add a fallback to `dveManager.GetNode()` when looking up a DVE by ID.
- **[gotcha]** Go's `html/template.ExecuteTemplate(w, "public_page.gohtml", data)` requires a template block named exactly `"public_page.gohtml"` to exist in the parsed template set. If a `.gohtml` file only defines inner sub-blocks (`{{ define "header" }}`, `{{ define "content" }}`, etc.) but no top-level block with that filename, the call silently fails or errors. Always add `{{ define "public_page.gohtml" }}{{ template "header" . }}{{ template "content" . }}{{ end }}` at the top of any template file that is referenced by name in `ExecuteTemplate`.
- **[fix]** Registering a named Gin route (e.g. via `registerGatewayPrefix("/api/agent", "/api/agent")`) for a prefix that overlaps with the existing `/api/*path` catch-all PANICS at startup: `"catch-all wildcard '*path' conflicts with existing path segment 'agent'"`. To route `/api/agent/` traffic to KNIRVGATEWAY without triggering this panic, add an early-return `strings.HasPrefix(c.Request.URL.Path, "/api/agent/")` block INSIDE the existing `api.Any("/*path", ...)` catch-all handler — the same pattern used for `/api/network-monitor/`. Do NOT attempt named route registration alongside the catch-all.
- **[gotcha]** Inner agent REST calls (`/api/v1/dve/{dveId}/inner/*`) are registered by the backend subprocess's `InnerAgentHandlers` and proxy to the per-DVE KNIRVAGENT Unix socket (`agent-{dveId}.sock`). Inner agent WebSocket streams (`/ws/dve/{dveId}/inner/*`) are registered by `DVEInnerAgentWSHandler`. Both are served by the backend subprocess — NOT by KNIRVGATEWAY's `dynamicAgentProxy`. Route `/api/agent/` (not `/api/v1/dve/.../inner/`) traffic to the gateway via the HasPrefix pattern above.
- **[correction]** Inner agent frontend API calls must use `/api/v1/dve/${dveId}/inner/...` (e.g. `/spawn`, `/sessions`, `/tools`, `/install`, `/sessions/{sessionId}/input`). The pattern `/api/agent/${dveId}/api/inner/...` is wrong — it targets a non-existent path structure and silently 404s.
- **[gotcha]** KNIRVGATEWAY's `dynamicAgentProxy` reads per-DVE agent socket paths from `AgentSocketDir` (set via `AGENT_SOCKET_DIR` env var, defaults to empty string). KNIRVSERVER `main.go` does not pass `AGENT_SOCKET_DIR` to the gateway subprocess — if unset at the OS level, `dynamicAgentProxy` cannot locate agent sockets and all `/api/agent/` proxying silently fails.
- **[convention]** The inner agent WebSocket URL must be `/ws/dve/{dveId}/inner/stream/{sessionId}` — the literal `dve` path segment after `/ws/` is required. The older pattern `/ws/{dveId}/inner/...` (without `dve`) routes to a dead path and the connection silently fails.
- **[pattern]** `main.go`'s `wsHandler` routes ALL `/ws/*` traffic directly through `wsProxy` (the backend's gorilla/mux over its Unix socket). There is no separate `gatewayWSProxy` for inner agent WS streams — it was removed. Do not add KNIRVGATEWAY routing for `/ws/dve/` paths; the backend handles them end-to-end.
- **[gotcha]** `AgentManager.GetSocketPath()` only succeeds for agents started by the current server process (in-memory `am.agents` map). After KNIRVSERVER restarts while KNIRVAGENT processes keep running, any `/api/v1/dve/{dveId}/inner/*` call returns 502 with "no agent running for DVE X" even though the agent socket is alive on disk. The disk fallback in `GetSocketPath` (check `agent-{dveID}.sock` in `socketDir`) now auto-recovers this — but `socketDir` must be configured or the fallback also fails.
- **[pattern]** The `make backend` target in `packages/KNIRVSERVER/Makefile` compiles `backend/cmd/backend_server`, copies the binary to `bin/`, then gzip-compresses it in place. After any `backend/` changes, run `make backend` from `packages/KNIRVSERVER/` — not just `go build ./...` inside `backend/` — before building the main binary, otherwise the embedded binary (`//go:embed bin/backend_server`) is stale.
- **[correction]** Inner agent "+" button in ConsolePanel should open a bare shell session immediately, not a tool-picker modal requiring GUI install. Add a `"shell"` entry to `DefaultCatalog()` in `packages/KNIRVAGENT/pkg/inner/catalog.go` (binary: `bash` or `sh`, empty `InstallCmd`) so the spawn endpoint opens a bare terminal without an install flow.
- **[gotcha]** `Tool.Install()` in `packages/KNIRVAGENT/pkg/inner/catalog.go` indexes `t.InstallCmd[0]` without a length check — panics for tools with no install command. Always guard: `if len(t.InstallCmd) == 0 { return nil }` before accessing the slice.
- **[gotcha]** Bash tool CWD persists across all calls in a session. A `cd packages/KNIRVAGENT` embedded in one bash command permanently shifts CWD for all subsequent Bash calls — relative paths like `ls packages/KNIRVSERVER/...` then silently return empty. Use absolute paths when working across packages, or verify with `pwd`.
- **[fix]** When `mcp__plugin_socraticode_socraticode__codebase_search` returns empty `{}` for any query in this project, the index is likely stale. Fall back immediately to `find <pkg_dir> -type f -name "*.go" | xargs grep -l <term>` — do not retry codebase_search with rephrased queries.
- **[gotcha]** `packages/KNIRVSERVER/backend/cmd/backend_server/main.go` is too large to read directly with the Read tool (returns empty `{}`). Use targeted grep: `grep -n "<pattern>" .../backend/cmd/backend_server/main.go | head -40`.
- **[gotcha]** KNIRVSERVER frontend tests use Jest 30 (`jest: ^30.1.0`), not Vitest. Always use native Jest API (`jest.fn()`, `jest.useFakeTimers()`, `jest.clearAllMocks()`) — do not import `vi` from `@jest/globals`.
- **[fix]** Running `npx jest --testPathPattern=...` in `packages/KNIRVSERVER/frontend/` silently returns empty `{}`. Use `npm run test -- --testPathPattern=<pattern>` or `npm test` instead.
- **[pattern]** New KNIRVSERVER system-level API endpoints needing `ServerApp` state must be added as early-return cases inside `main.go`'s `api.Any("/*path", ...)` catch-all handler — the backend `APIRouter` cannot reach `ServerApp`.
- **[gotcha]** The `DVEURIRegistry` (`backend/internal/services/dve_uri_registry.go`) is an in-memory map that starts empty on every boot — DVEs tracked via `dveManager.NodeTracker` are NOT registered in it. Always add a fallback to `dveManager.GetNode()` when looking up a DVE by ID.
- **[gotcha]** Go's `html/template.ExecuteTemplate(w, "public_page.gohtml", data)` requires a template block named exactly `"public_page.gohtml"`. If the file only defines inner sub-blocks, the call silently fails. Always include a top-level `{{ define "public_page.gohtml" }}` block in any template referenced by name in `ExecuteTemplate`.
- **[fix]** Registering a named Gin route for a prefix that overlaps the existing `/api/*path` catch-all PANICS at startup with `"catch-all wildcard conflicts with existing path segment"`. Route new prefixes via an early-return `strings.HasPrefix(c.Request.URL.Path, "/api/X/")` block INSIDE the catch-all handler — not via named route registration.
- **[gotcha]** Inner agent REST (`/api/v1/dve/{dveId}/inner/*`) and WS (`/ws/dve/{dveId}/inner/*`) are both handled by the backend subprocess, NOT by KNIRVGATEWAY's `dynamicAgentProxy`. Do not add KNIRVGATEWAY routing for these paths.
- **[correction]** Inner agent frontend API calls must use `/api/v1/dve/${dveId}/inner/...`. The pattern `/api/agent/${dveId}/api/inner/...` is wrong and silently 404s.
- **[gotcha]** KNIRVGATEWAY's `dynamicAgentProxy` requires the `AGENT_SOCKET_DIR` env var to locate per-DVE agent sockets. If unset at the OS level, all `/api/agent/` proxying silently fails.
- **[convention]** Inner agent WebSocket URL must be `/ws/dve/{dveId}/inner/stream/{sessionId}` — the `dve` segment after `/ws/` is required. The pattern `/ws/{dveId}/inner/...` routes to a dead path.
- **[pattern]** `main.go`'s `wsHandler` routes ALL `/ws/*` traffic through `wsProxy` (backend's Unix socket). Do not add KNIRVGATEWAY routing for `/ws/dve/` paths — the backend handles them end-to-end.
- **[gotcha]** `AgentManager.GetSocketPath()` only succeeds for agents in the current process's in-memory map. After KNIRVSERVER restarts, calls return 502 even if the agent socket is alive on disk. The disk fallback requires `socketDir` to be configured.
- **[pattern]** The `make backend` target in `packages/KNIRVSERVER/Makefile` compiles, copies, and gzip-compresses `backend/cmd/backend_server`. After any `backend/` changes, run `make backend` from `packages/KNIRVSERVER/` — `go build ./...` inside `backend/` alone leaves the embedded binary stale.
- **[fix]** `go build` and `go test` for KNIRVSERVER backend must be run from `packages/KNIRVSERVER/backend/` (the Go module root). Running `go build ./backend/...` or `go test ./backend/tests/...` from `packages/KNIRVSERVER/` silently returns empty output with no error — the parent directory is not a Go module. Always run from inside `packages/KNIRVSERVER/backend/`.
- **[gotcha]** In `DVEManager`, `GetNodes` reads node status from BuntDB while `checkNodeHealth` reads from the in-memory `nodeTracker`. Heartbeat routines must update BOTH: call `nodeTracker.UpdateHeartbeat(nodeID)` AND persist updated `Status` + `LastHeartbeat` to BuntDB via `storeNode()`. Updating only the tracker keeps nodes alive in the health monitor but they vanish from the KNIRVGATEWAY WebGUI list (which queries DB via `/api/dve-nodes`).
