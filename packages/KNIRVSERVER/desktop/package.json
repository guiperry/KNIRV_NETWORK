{
  "name": "knirv-desktop-hud",
  "version": "1.0.0",
  "description": "KNIRV Desktop HUD - Frame for KNIRVSERVER frontend",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && cp index.html dist/ && cp styles.css dist/ && cp renderer.js dist/",
    "start": "npm run build && electron .",
    "dev": "npm run build && electron ."
  },
  "keywords": ["electron", "desktop", "hud", "knirv"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "electron": "^28.0.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {}
}
