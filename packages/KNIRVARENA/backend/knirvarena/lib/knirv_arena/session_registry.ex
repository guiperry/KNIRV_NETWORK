defmodule KnirvArena.SessionRegistry do
  @moduledoc """
  Registry for tracking ResolutionSession GenServers.
  """
  
  # In Elixir 1.12.2, we define the registry differently
  # We'll start it as part of the supervision tree instead of using __using__
end