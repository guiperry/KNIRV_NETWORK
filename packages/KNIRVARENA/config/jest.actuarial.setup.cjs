// The curated-posting selector is pure and requires no DOM/network mocks.
// Keep this JavaScript setup file so Jest does not parse the legacy TypeScript
// global-polyfill file with its untyped setup transformer.
