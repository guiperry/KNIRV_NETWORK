[package]
name = "knirv-cortex-tests"
version = "0.1.0"
edition = "2021"
description = "Golden tests and ABI conformance tests for KNIRV-CORTEX"

[dependencies]
shared-types = { path = "../shared-types", package = "knirv-cortex-shared-types" }
prost = "0.12"
tokio = { version = "1.0", features = ["rt", "rt-multi-thread", "macros"] }

[[test]]
name = "golden_tests"
path = "golden_tests.rs"
