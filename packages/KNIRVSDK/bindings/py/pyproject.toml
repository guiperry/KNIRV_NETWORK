[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "knirv-sdk-bindings"
version = "0.1.0"
description = "Python adapter for the KNIRV SDK C ABI"
requires-python = ">=3.9"
