The application is crashing with a SIGABRT signal during CGo execution within the github.com/getlantern/systray package, specifically in _Cfunc_nativeLoop. This often indicates an issue with how the systray's native event loop is being run, usually related to threading. GUI libraries like systray typically have strict requirements about running on the main OS thread or having their event loop managed correctly.

Looking at the stack trace for goroutine 290, it's clear the crash originates from systray.Run. The log also shows a LIBDBUSMENU-GLIB-WARNING just before the crash, which can be a symptom of the systray not interacting correctly with the desktop environment, possibly due to improper initialization.

The most likely cause of this specific crash is how systray.Run() is being invoked within your SystrayManager.Run method. If systray.Run() (which is a blocking call that starts the native event loop for the systray) is called inside a nested goroutine, while the outer function (SystrayManager.Run) continues to do other things or exits, the systray's native components might not be set up or serviced correctly on the thread they expect.

Here's a proposed fix for systray.go to correctly invoke systray.Run():

Fix in systray.go:

The SystrayManager.Run method should call systray.Run() directly. The systray.Run() function itself is blocking and will manage the systray's lifecycle. The onReady callback is the place to set up menu items, and the click handlers for these items (which use channels) should be processed in a separate goroutine, as is standard practice with this library.

diff
-22
+64
The code change produced by Gemini could not be fully parsed. Can't automatically accept changes.
	nodeManagerGUIToggleChan chan bool
}

// NewSystrayManager creates a new systray manager.
// NewSystrayManager creates a new systray manager. `guiMgr` can be nil if no GUI is initially available.
func NewSystrayManager(cfg *config.Config, nodeMgr *NodeManager, guiMgr IGUI) *SystrayManager {
	return &SystrayManager{
		cfg:                      cfg,
		nodeManager:              nodeMgr,
		guiManager:               guiMgr,
		menuItems:                make(map[string]*systray.MenuItem),
		nodeManagerGUIToggleChan: make(chan bool), // Initialize channel for GUI toggle
		cfg:         cfg,
		nodeManager: nodeMgr,
		guiManager:  guiMgr,
		menuItems:   make(map[string]*systray.MenuItem),
		// nodeManagerGUIToggleChan is used to signal GUI changes.
		// Its usage depends on how GUI interaction is designed.
		// For now, let's assume it's for a specific type of GUI interaction.
		nodeManagerGUIToggleChan: make(chan bool),
	}
}

// Run starts the systray manager. This function is blocking.
func (sm *SystrayManager) Run(onExit func()) {
	go func() { // This inner goroutine is problematic for systray.Run
		systray.Run(sm.onReady, onExit) // systray.Run is blocking
	}()
	// The original for/select loop here would not work as expected with systray.Run in a separate goroutine.
	// Click handling is done via channels from menu items, typically in a goroutine started in onReady.
	// This function will now block until systray.Quit() is called.
	// systray.Run is blocking and must be called directly.
	// It takes an onReady and onExit callback.
	systray.Run(sm.onReady, func() { // This is systray's onExit
		log.Println("Systray internal onExit triggered.")
		if onExit != nil {
			onExit() // Call the onExit callback provided to SystrayManager.Run
		}
	})
	log.Println("SystrayManager.Run: systray.Run has exited.")
}

func (sm *SystrayManager) onReady() {
	// mStopMining := systray.AddMenuItem("Stop Mining", "Stop mining blocks")
	// mStopMining.Disable() // Initially disabled

	// TODO: Add more items like "Open Wallet", "Network Status", etc.

	mQuit := systray.AddMenuItem("Quit", "Quit KNIRVROOT")

	// Handle clicks in a loop
	// Goroutine to handle menu item clicks. This is the standard pattern.
	// systray.Run() handles dispatching events to these channels.
	go func() {
		for {
			select {
			case <-mOpenWebGUI.ClickedCh:
				sm.openWebInterface()

			case <-mShowHideGUI.ClickedCh:
				log.Println("Systray: Show/Hide GUI clicked")
				if sm.guiManager != nil {
					// This assumes guiManager has a method to toggle visibility.
					// For altgui, this would involve dispatching to the webview thread.
					sm.guiManager.ToggleVisibility() // Example: you'll need to implement this in IGUI and altgui.GUI
					// Example: sm.guiManager.ToggleVisibility()
					// For now, using the channel as an example if another part of the app listens to it.
					// A direct method call on guiManager is often cleaner.
					select {
					case sm.nodeManagerGUIToggleChan <- true:
						log.Println("Systray: Sent GUI toggle signal.")
					default:
						log.Println("Systray: GUI toggle channel busy or no listener.")
					}
				} else {
					log.Println("Systray: No GUI manager available to toggle.")
				}

			// case <-mStartMining.ClickedCh:
			// 	sm.handleStartMining(mStartMining, mStopMining)

			// case <-mStopMining.ClickedCh:
			// 	sm.handleStopMining(mStartMining, mStopMining)

			case <-mViewLogs.ClickedCh:
				sm.viewLogs()
			case <-mOpenDataDir.ClickedCh:
				sm.openDataDirectory()
			case <-mQuit.ClickedCh:
				log.Println("Systray: Quit clicked")
				systray.Quit() // This signals systray.Run to exit, which then calls its onExit.
	}()
}

// SetGUIManager allows updating the GUI manager instance, e.g., after AltGUI is initialized.
func (sm *SystrayManager) SetGUIManager(guiMgr IGUI) {
	sm.guiManager = guiMgr
}

func (sm *SystrayManager) getIcon(s string) []byte {
	// Check embedded first
	if s == "images/icon.png" || s == "icon.png" { // Adjust based on actual key if embedded
		log.Printf("Systray: Failed to open web interface: %v", err)
	}
}

func (sm *SystrayManager) viewLogs() {
	logDir, err := config.GetLogDir()
	if err != nil {
		log.Printf("Systray: Error getting log directory: %v", err)
		return
	}
	logFilePath := filepath.Join(logDir, "KNIRVROOT.log")
	if err := open.Run(logFilePath); err != nil {
		log.Printf("Systray: Failed to open log file %s: %v", logFilePath, err)
	}
}

func (sm *SystrayManager) openDataDirectory() {
	// Assuming Root role for now, or get role from config
	dataDir, err := config.GetAppDataDir(sm.cfg.Role)
	if err != nil {
		log.Printf("Systray: Error getting data directory: %v", err)
		return
	}
	if err := open.Run(dataDir); err != nil {
		log.Printf("Systray: Failed to open data directory %s: %v", dataDir, err)
+	}
+}

Explanation of systray.go changes:

SystrayManager.Run(onExit func()):
The problematic inner go func() { systray.Run(...) }() has been removed.
systray.Run(sm.onReady, internalOnExit) is now called directly. This is a blocking call and will keep the goroutine that SystrayManager.Run is executing in alive until systray.Quit() is called.
The onExit callback provided to SystrayManager.Run is now correctly passed to be called when systray.Run itself exits.
SystrayManager.onReady():
The go func() { for { select { ... } } }() loop for handling menu item clicks remains. This is the correct pattern: systray.Run manages the native event loop, and your Go code listens on channels for clicks.
Added SetGUIManager method to allow main.go to update the guiManager reference if it's initialized after the SystrayManager.
Added placeholder viewLogs and openDataDirectory methods and corresponding menu items.
Improved the mShowHideGUI click handler to be more robust if guiManager is nil or the channel send would block.
Further considerations for main.go and altgui.go (important for overall GUI stability but perhaps not the direct cause of this crash):

Main Thread for WebView: Ensure that webview.Run() (called via altguiInstance.Run()) is executed on the main OS thread. You can achieve this by calling runtime.LockOSThread() at the beginning of your main.main() function and ensuring that the altguiInstance.Run() call happens directly in main.main() or a goroutine that main.main() effectively blocks on for its primary GUI loop.
go
// main.go
func main() {
    runtime.LockOSThread() // Lock main goroutine to OS thread for GUI
    // ...
    if cfg.UseAltGUI && guiInstance != nil {
        guiInstance.Run() // This should call webview.Run() and block here
    } else {
        mainWg.Wait() // Wait if no primary GUI loop on main thread
    }
    // runtime.UnlockOSThread() // If needed
}
altgui.InitializeGUI: This function should not start the webview.Run() loop in a goroutine. It should only initialize the GUI object. The Run() method of the GUI object should be called from main.go as described above.
By applying the diff to systray.go, you should resolve the immediate crash related to systray.Run(). Then, review your main.go and altgui.go to ensure webview is also handled correctly regarding its main thread requirements.

Rate this answer: 
Context Sources (9)

Incline Admin
Please see my open systray.go file and give me a clean diff, your previous diff is broken.


Gemini
Okay, I've reviewed the systray.go file you provided. The main issues are:

The SystrayManager struct contains fields (cfg, nodeManager, guiManager, menuItems, nodeManagerGUIToggleChan) that are not initialized by its current NewSystrayManager constructor and are not used by the methods in this specific version of the file. This would lead to compile errors or nil pointer issues if they were accessed.
The Run method needs to correctly manage the lifecycle, especially how systray.Quit() is called and how it interacts with the main application's shutdown (cancelFunc).
I'll provide a "clean" diff that addresses these points, making the systray.go consistent with its current usage by altgui.go (managing webview visibility and application quit).

Here's the proposed diff:

diff
-30
+30
The code change produced by Gemini could not be fully parsed. Can't automatically accept changes.
	isWebviewHidden bool
	cancelFunc      context.CancelFunc
	wg              *sync.WaitGroup
	quitChan        chan struct{}
	showDialogChan  chan struct{}
	cfg                      *config.Config 
	nodeManager              *NodeManager   
	guiManager               IGUI           
	menuItems                map[string]*systray.MenuItem 
	nodeManagerGUIToggleChan chan bool      
	// Channels for internal signaling
	quitChan       chan struct{} // Closed to signal application quit
	showDialogChan chan struct{} // Receives a signal to show the confirmation dialog

	// Unused fields removed for clarity in this version:
	// cfg, nodeManager, guiManager, menuItems, nodeManagerGUIToggleChan
}

// NewSystrayManager creates a new SystrayManager and binds JS functions
		wg:              wg,
		quitChan:        make(chan struct{}),
		showDialogChan:  make(chan struct{}),
		cfg:         cfg,
		nodeManager: nodeMgr,
		guiManager:  guiMgr,
		menuItems:   make(map[string]*systray.MenuItem),
		// nodeManagerGUIToggleChan is used to signal GUI changes.
		// Its usage depends on how GUI interaction is designed.
		// For now, let's assume it's for a specific type of GUI interaction.
		nodeManagerGUIToggleChan: make(chan bool),
	}

	// Bind Go methods to JavaScript
	return sm
}

// Run starts the system tray
func (sm *SystrayManager) Run(onExit func()) {
// Run starts the system tray. This function is blocking and should typically be run in a goroutine.
func (sm *SystrayManager) Run() {
	// Goroutine to listen on quitChan. When quitChan is closed, it calls systray.Quit().
	go func() {
		<-sm.quitChan
		log.Println("SystrayManager: Received quit signal from application logic. Telling systray to quit.")
		systray.Quit() // This will trigger the onExit callback passed to systray.Run below.
	}()

	// Goroutine to listen on showDialogChan for showing the confirmation dialog.
	go func() {
		for range sm.showDialogChan {
			sm.showConfirmationDialog()
		}
	}()

	// systray.Run is blocking and must be called directly.
	// It takes an onReady and onExit callback.
	// The onExit callback is executed when systray.Quit() is called and the native event loop terminates.
	systray.Run(sm.onReady, func() { // This is systray's onExit
		log.Println("Systray internal onExit triggered.")
		if onExit != nil {
			onExit() // Call the onExit callback provided to SystrayManager.Run
		log.Println("SystrayManager: systray.Run onExit callback triggered (systray is quitting).")
		if sm.cancelFunc != nil {
			log.Println("SystrayManager: Calling main context cancelFunc to signal application shutdown.")
			sm.cancelFunc() // Signal the rest of the application to shut down.
		}
	})
	log.Println("SystrayManager.Run: systray.Run has exited.")

	// Listen for show dialog signal
	go func() {
		for range sm.showDialogChan {
			sm.showConfirmationDialog()
		}
	}()
	log.Println("SystrayManager.Run: systray.Run has completed. Systray has exited.")
}

// onReady is called when the system tray is ready
				}
			case <-mQuit.ClickedCh:
				log.Println("Quit clicked")
				// Signal the quit channel
				close(sm.quitChan)
				// Closing quitChan signals the goroutine in sm.Run() to call systray.Quit()
				close(sm.quitChan) 
 				return
 			}
 		}

Explanation of Changes:

SystrayManager Struct:

Removed the fields cfg, nodeManager, guiManager, menuItems, and nodeManagerGUIToggleChan as they are not initialized by the current NewSystrayManager (the one called from altgui.go) and are not used by the methods in this file. This makes the struct reflect its actual usage in this context.
Added comments to clarify the purpose of quitChan and showDialogChan.
NewSystrayManager Function:

Removed the erroneous initialization of the fields mentioned above (e.g., cfg: cfg,). These were causing compile errors because cfg, nodeMgr, guiMgr are not defined in that function's scope.
Run Method:

The onExit func() parameter was removed as it wasn't being used by altgui.go. The shutdown logic is now self-contained using sm.cancelFunc.
A new goroutine is started to listen on sm.quitChan. When this channel is closed (either by the "Quit" menu item or the quitApplication JS binding), it calls systray.Quit().
systray.Run is called. Its onExit callback (the second argument) is now responsible for calling sm.cancelFunc(). This onExit is triggered when systray.Quit() has successfully terminated the native systray event loop.
The goroutine for sm.showDialogChan is kept as it correctly handles showing the confirmation dialog.
How to use with altgui.go:

In your altgui.go file, within the createWebViewWindow method, you should run the SystrayManager in a goroutine because systray.Run() (and thus sm.Run()) is blocking:

go
// In altgui.go, inside func (g *GUI) createWebViewWindow()

// ... (webview setup) ...

// Initialize the systray manager
g.systrayManager = NewSystrayManager(w, g.cancelFunc, g.wg)

// Run the systray manager in a goroutine
if g.wg != nil {
    g.wg.Add(1) // If you're using a WaitGroup for graceful shutdown
}
go func() {
    if g.wg != nil {
        defer g.wg.Done()
    }
    log.Println("Starting SystrayManager in a goroutine...")
    g.systrayManager.Run()
    log.Println("SystrayManager goroutine finished.")
}()

// ... (webview bindings like handleWindowClose) ...

w.Run() // This will now execute because systrayManager.Run() is in a goroutine
This setup ensures that:

The systray.go file is internally consistent and free of unused/uninitialized members for its current role.
The application shutdown is correctly signaled via cancelFunc when the systray is quit.
The webview and systray can run their event loops concurrently.