*   Fully implement the <plan> Implementation Flow as detailed in the <plan>_Implementation.md document. Do not drift from this document. Do not regress or change the fundamental functionality of the existing application aside from what is detailed in the document. Log as you go. 

*   Draft a detailed implementation plan for this new feature. Write it in .md format. Do not execute the implementation yet:

We need a new -bootnode flag that will allow this application to startup as a bootnode. Add a new command line option called -bootnode which will enable the node to act as a bootnode. This means, that it should be able to accept connections from other nodes without having any devs. It should also have a way to advertise itself on the network so that other nodes can connect to it. It should be able to operate as a background system process and restart automatically when the system reboots. The bootnode will register (and stay in active status) itself with the registry server for discovery by other nodes. This registry service has been implemented separately and is already up and running. Please see the 11_Bootnode_Registry_Service.md file for reference. We may need a way to test this service for availability before attempting to use it. If we cannot reach the registry service then we should just start the DHT discovery manager and try to connect to the hardcoded bootstrap devs listed as `KNIRVCHAINBootstrapPeers` via NewDiscoveryManager. The bootnode should also be able to handle incoming requests for dev discovery and respond accordingly.


Confirm that we've fully implemented Phase 1 of the Role-based Initialization and Config Refactor Plan. If it has not been done, then you need to fully implement Phase 1 of the Business_Logic_Implementation Plan as detailed in the Init_REfactor_Plan.md document. Do not drift from this document. Do not regress or change the fundamental functionality of the existing application aside from what is detailed in the document. Log as you go. 