---
# Role metadata for factuality-slice

galaxy_info:
  author: KNIRV Network Team
  description: Ansible role for deploying KNIRV Factuality Slice Capability Node
  company: KNIRV Network
  license: MIT
  min_ansible_version: 2.9
  
  platforms:
    - name: Ubuntu
      versions:
        - focal
        - jammy
    - name: CentOS
      versions:
        - 8
        - 9
    - name: Amazon
      versions:
        - 2023

  galaxy_tags:
    - knirv
    - blockchain
    - capability
    - factuality
    - verification
    - deployment
    - health-check

# Role dependencies
dependencies:
  # This role should run after the main KNIRV services are deployed
  # No explicit dependencies defined as this is handled by playbook ordering
